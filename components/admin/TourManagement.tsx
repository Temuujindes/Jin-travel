'use client'

import Image from 'next/image'
import Link from 'next/link'
import { Grid2X2, List, Plus, Search, SlidersHorizontal, X } from 'lucide-react'
import { FormEvent, useMemo, useState } from 'react'
import { tours as seedTours } from '../../lib/mock-data/tours'
import { Tour, TourStatus } from '../../lib/types'
import { localizedTitle } from '../../lib/tour-utils'
import { useTranslation } from '../LanguageContext'
import { AdminShell } from './AdminShell'
import { AdminPageHeader, PanelHeading } from './AdminPanel'

export default function TourManagement() { const { t } = useTranslation(); const [tours, setTours] = useState<Tour[]>(seedTours.map((tour, index) => ({ ...tour, status: index === 2 ? 'draft' : 'active', featured: index === 0 }))); const [query, setQuery] = useState(''); const [status, setStatus] = useState<'all' | TourStatus>('all'); const [view, setView] = useState<'list' | 'grid'>('list'); const [dialog, setDialog] = useState(false); const visibleTours = useMemo(() => tours.filter((tour) => `${tour.title} ${tour.slug}`.toLowerCase().includes(query.toLowerCase()) && (status === 'all' || tour.status === status)), [query, status, tours]); const addTour = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const title = String(form.get('title') || t('newTour')); const newTour: Tour = { ...seedTours[0], slug: title.toLowerCase().replace(/[^a-z0-9]+/g, '-'), title, subtitle: String(form.get('description') || ''), duration: String(form.get('duration') || '4 Days 3 Nights'), priceUsd: Number(form.get('price') || 0), status: 'draft', featured: false }; setTours((current) => [newTour, ...current]); setDialog(false) }; return <AdminShell><AdminPageHeader overline={t('catalog')} title={t('tourPackages')} description={t('manageTours')} action={<button className="admin-button" onClick={() => setDialog(true)}><Plus size={16} />{t('addTour')}</button>} /><div className="toolbar"><div className="search-box"><Search size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder={t('searchTours')} aria-label={t('searchTours')} /></div><div className="filter-buttons"><button className={status === 'all' ? 'filter-active' : ''} onClick={() => setStatus('all')}>{t('all')}</button><button className={status === 'active' ? 'filter-active' : ''} onClick={() => setStatus('active')}>{t('active')}</button><button className={status === 'draft' ? 'filter-active' : ''} onClick={() => setStatus('draft')}>{t('draft')}</button></div><div className="view-toggle"><button className={view === 'list' ? 'selected' : ''} onClick={() => setView('list')} aria-label="List view"><List size={17} /></button><button className={view === 'grid' ? 'selected' : ''} onClick={() => setView('grid')} aria-label="Grid view"><Grid2X2 size={17} /></button></div></div><section className={`admin-panel tour-management ${view === 'grid' ? 'grid-view' : ''}`}><div className="table-label"><span>{visibleTours.length} {t('journeys')}</span><span><SlidersHorizontal size={14} /> {t('updatedRecently')}</span></div>{visibleTours.map((tour) => <article className="tour-admin-row" key={tour.slug}><Image src={tour.image} alt={tour.title} width={92} height={68} /><div className="tour-admin-name"><strong>{localizedTitle(tour, 'en')}</strong><span>{tour.slug}</span></div><div className="language-state"><span>MN</span><span>KR</span><span>EN</span></div><span className="admin-duration">{tour.duration}</span><strong className="admin-price">${tour.priceUsd}</strong><span className={`admin-tour-status ${tour.status}`}>{tour.status === 'active' ? t('active') : t('draft')}</span><div className="tour-actions"><button onClick={() => setTours((current) => current.map((item) => item.slug === tour.slug ? { ...item, status: item.status === 'active' ? 'draft' : 'active' } : item))}>{tour.status === 'active' ? t('makeDraft') : t('publish')}</button><Link href={`/dashboard/tours/builder?tour=${tour.slug}`}>{t('edit')}</Link></div></article>)}</section>{dialog && <div className="admin-dialog-backdrop"><form className="admin-dialog" onSubmit={addTour}><PanelHeading className="dialog-heading" overline={t('catalogEntry')} title={t('newTour')} action={<button type="button" onClick={() => setDialog(false)} aria-label={t('cancel')}><X size={18} /></button>} /><label>{t('title')} MN<input name="title" required placeholder={t('newTour')} /></label><label>{t('description')}<input name="description" placeholder={t('description')} /></label><div className="form-two"><label>{t('duration')}<input name="duration" defaultValue="4 Days 3 Nights" /></label><label>{t('priceUsd')}<input name="price" type="number" min="0" defaultValue="580" /></label></div><label>{t('mainImage')} URL<input name="image" placeholder="https://..." /></label><div className="dialog-actions"><button type="button" className="admin-quiet-button" onClick={() => setDialog(false)}>{t('cancel')}</button><button className="admin-button" type="submit">{t('save')}</button></div></form></div>}</AdminShell> }
